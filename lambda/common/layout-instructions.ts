export const FIXED_LAYOUT_INSTRUCTIONS = `
【最重要・絶対厳守】以下を1つでも破ってはいけない:
・2×2、2×3、3×2のような格子(グリッド)状のコマ割りは絶対禁止。コマを横に並べたり、2段以上にまたがって配置してはいけない。
・4コマを2×2ではなく、縦一列に上から下へ順番に並べること。
・画面全体は縦長の1枚の画像とし、
 コマ1→コマ2→コマ3→コマ4の順に上から下へ配置する。
・各コマの間には区切り線を入れ、コマの縦幅は均等にすること。
・イメージとしては、縦に4段重ねた短冊(縦長のコミックストリップ)のような構図にすること。

【セリフの文字について】
・セリフは短く簡潔にし、指定された日本語のセリフを一字一句正確に描画すること。誤字・存在しない漢字・意味不明な文字列を生成しないこと。

【色について】
・カラーではなく白黒(モノクロ)のイラストにすること。色を一切使わず、線画・グレースケールのみで表現すること。

【画風について】
・デジタルできれいに整えたイラストではなく、手描き風(手書き風)の絵にすること。鉛筆やペンで描いたような、線に強弱やゆらぎのあるラフなタッチにすること。
`.trim();
